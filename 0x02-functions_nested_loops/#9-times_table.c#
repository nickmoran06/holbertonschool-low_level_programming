#include "holberton.h"
/**
 * times_table - Table of 1 until 9
 *
 *
 * Return: any
 */
void times_table(void)
{
	int line, colum;

	for (line = 0, line <= 9; line++)
	{
		for (colum = 0; colum <= 9; colum++)
		{
			if (line * colum < 10)
			{
_putchar
			}
			if else (line * colum > 10)
			{

		       	}
		}
		_putchar(10);
	}
}
