#include "holberton.h"
/**
 * cap_string - capitalizes all words
 * @a: the variable
 * Return: string to success
 */
char *cap_string(char *n)ç∂ç√git status


{
	unsigned int a = 0 ,a = 0;

	while (haystack[i])
	{
		while (needle[j] && (haystack[i] == needle[0]))
		{
			if (haystack[i + j] == needle[j])
				j++;
			else
				break;
		}
		if (needle[j])
		{
			i++;
			j = 0;
		}
		else
			return (haystack + i);
	}
	return (0);
}
