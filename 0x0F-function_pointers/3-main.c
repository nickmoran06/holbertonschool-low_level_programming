#include "3-calc.h"

/**
 * Main - entry point
 *
 * Return: integer
 */
int main(argc, **argv)
{
	int i;
	int j;
	int k;

	i = atoi(argv[1]);
	j = atoi(argv[3]);
	k = f(a, b);
	if (argc != 4)
	{
		printf("Error\n");
		exit(98);
	}

}
